# Core runtime dependencies
# Pinned versions for security and stability (updated 2026-01)
#
# SECURITY NOTE: The following system packages should be upgraded to fix CVEs:
# - cryptography: Upgrade to >=43.0.1 (fixes CVE-2024-26130, CVE-2023-50782, CVE-2024-0727)
# - setuptools: Upgrade to >=78.1.1 (fixes CVE-2025-47273, CVE-2024-6345)
# - pip: Upgrade to >=25.3 (fixes CVE-2025-8869)
# Run: pip install --upgrade cryptography>=43.0.1 setuptools>=78.1.1 pip>=25.3

# TOML parsing for Python < 3.11
tomli>=2.0.0,<3.0.0; python_version < "3.11"

# YAML configuration parsing
# CVE-2020-14343 fixed in 5.4+
pyyaml>=6.0.1,<7.0.0

# Async HTTP client and WebSocket support
# Multiple CVEs fixed in recent versions
aiohttp>=3.9.0,<4.0.0

# WebSocket protocol implementation
websockets>=12.0,<17.0

# Data validation and schema enforcement
pydantic>=2.0.0,<3.0.0

# OS keychain support for credentials
keyring>=25.7.0,<26.0.0

# Test/development helpers
pytest>=7.4.0,<10.0.0
pytest-asyncio>=0.21.0,<2.0.0
